server:
  port: 8080

spring:
  application:
    name: teacher-server
  datasource:
    url: jdbc:mysql://localhost:3306/teacher_service?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 20050921
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

app:
  jwt:
    secret: teacher-platform-jwt-secret-change-in-production
    expire-hours: 72
